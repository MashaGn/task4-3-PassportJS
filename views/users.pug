doctype html
html
   head
       meta(charset='utf-8')
       title Users
   body
       h1  Users
       table
           tbody
               if users.length
                   each user in users
                       tr
                           td= user.login
                           td= user.password
